{% for item in items|default %}
    {% include '@OswisOrgOswisCore/web/parts/sitemap-item.xml.twig'
        with {
        'path': item.path|default,
        'changeFrequency': item.changeFrequency|default,
        'priority': item.priority|default,
        'changed': item.changed|default,
        'isActuality': item.isActuality|default,
        'created': item.created|default,
        'title': item.title|default} %}
{% endfor %}