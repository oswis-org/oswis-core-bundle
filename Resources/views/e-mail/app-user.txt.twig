{% extends '@ZakjakubOswisCore/e-mail/message.txt.twig' %}

{% block title %}{% if data.type == 'activation-request' %}Vytvoření uživatelského účtu{% elseif data.type == 'activation' %}Aktivace uživatelského účtu{% else %}Změna uživatelského účtu{% endif %}{% endblock %}

{% block description %}{% if data.type == 'activation-request' %}Vytvoření uživatelského účtu{% elseif data.type == 'activation' %}Aktivace uživatelského účtu{% else %}Změna uživatelského účtu{% endif %}{% endblock %}

{% block content_content %}
Dobrý den,

{% if data.type == 'activation-request' %}
byl vytvořen nový uživatelský účet v informačním systému.
{% elseif data.type == 'activation' %}
Váš uživatelský účet byl aktivován.
{% else %}
došlo ke změně u Vašeho uživatelského účtu v informačním systému.
{% endif %}

{% if data.type == 'activation-request' and data.token %}
Ještě je nutné provést aktivaci uživatelského účtu. Aktivaci provedete na následujícím odkazu:
{{ absolute_url(path('zakjakub_oswis_core_app_user_activation', {'token': data.token})) }}
Odkaz je platný 12 hodin od vygenerování.
{% endif %}

Uživatelské jméno u uživatelského účtu je {{ data.appUser.username ? data.appUser.username : data.appUser.email }} a hlavní e-mail je nastaven na {{ data.appUser.email }}.
{% if data.password %}
Bylo nastaveno nové, náhodně vygenerované, heslo {{ data.password }}.
{% elseif data.appUser.password %}
Heslo bylo nastaveno při registraci.
{% else %}
Heslo si nastavíte při aktivaci účtu.
Pokud heslo zapomenete, můžete jej obnovit pomocí tlačítka na přihlašovací stránce aplikace.
{% endif %}

{% endblock %}


