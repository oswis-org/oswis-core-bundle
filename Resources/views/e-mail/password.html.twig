{% extends '@OswisOrgOswisCore/e-mail/message.html.twig' %}

{% block html_title %}{{ email.subject|default('Změna hesla u uživatelského účtu') }}{% endblock %}
{% block meta_description %}{{ block('html_title') }}{% endblock meta_description %}

{% block content_inner %}
    <p>
        u {{ f ? 'Vašeho' : 'Tvého' }} uživatelského účtu v informačním systému
        byl zaznamenán požadavek na změnu hesla pro přihlášení.
    </p>
    {% if type == 'password-change-request' %}
        <p>
            {% if tokenUrl is defined and tokenUrl %}
                Nové heslo nastaví{{ f ? 'te' : 'š' }} na adrese:
                <a href="{{ tokenUrl }}" title="Odkaz pro nastavení hesla" target="_blank">{{ tokenUrl }}</a>.<br>
                <small>Odkaz je platný 24 hodin od vygenerování.</small>
            {% elseif token is defined and token %}
                Token pro změnu hesla je: {{ token }}.<br>
                <small>Token je platný 24 hodin od vygenerování a použijete jej pro změnu hesla přímo v informačním systému.</small>
            {% else %}
                Odkaz pro změnu hesla se z neočekávaných důvodů nepodařilo vygenerovat.<br>
                <small>Kontaktuj{{ f ? 'te' : '' }} nás, prosím, pro vyřešení situace.</small>
            {% endif %}
        </p>
    {% elseif type == 'password-change' %}
        <p>
            Nové heslo bylo úspěšně nastaveno.<br>
            {% if password %}
                <strong>Nově nastavené heslo:</strong> {{ password }}
            {% endif %}
        </p>
    {% else %}
        <p>Při zpracování požadavku na změnu hesla došlo k chybě.</p>
    {% endif %}
{% endblock %}


