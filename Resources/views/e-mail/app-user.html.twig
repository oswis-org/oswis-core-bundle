{% extends '@ZakjakubOswisCore/e-mail/message.html.twig' %}
{% block title %}
    {% if data.type == 'activation-request' %}
        Vytvoření uživatelského účtu
    {% elseif data.type == 'activation' %}
        Aktivace uživatelského účtu
    {% else %}
        Změna uživatelského účtu
    {% endif %}
{% endblock %}
{% block description %}
    {% if data.type == 'activation-request' %}
        Vytvoření uživatelského účtu
    {% elseif data.type == 'activation' %}
        Aktivace uživatelského účtu
    {% else %}
        Změna uživatelského účtu
    {% endif %}
{% endblock %}
{% block content_content %}
    <tr>
        <td colspan="12">
            <p>
                Dobrý den,<br>

                {% if data.type == 'activation-request' %}
                    byl vytvořen nový uživatelský účet v informačním systému.<br>
                {% elseif data.type == 'activation' %}
                    Váš uživatelský účet byl aktivován.<br>
                {% else %}
                    došlo ke změně u Vašeho uživatelského účtu v informačním systému.
                {% endif %}
            </p>

            {% if data.type == 'activation-request' and data.token %}
                Ještě je nutné provést aktivaci uživatelského účtu. Aktivaci provedete na následujícím odkazu:<br>
                <a href="{{ absolute_url(path('zakjakub_oswis_core_app_user_activation', {'token': data.token})) }}" title="Odkaz pro aktivaci účtu"
                   target="_blank">{{ absolute_url(path('zakjakub_oswis_core_app_user_activation', {'token': data.token})) }}</a>.<br>
                <small>Odkaz je platný 24 hodin od vygenerování.</small>
            {% endif %}

            <p>
                Uživatelské jméno u uživatelského účtu je
                <strong>{{ data.appUser.username ? data.appUser.username : data.appUser.email }}</strong>
                a hlavní e-mail je nastaven na <strong>{{ data.appUser.email }}</strong>.
                {% if data.password %}
                    Bylo nastaveno nové, náhodně vygenerované, heslo <strong>{{ data.password }}</strong>.
                {% elseif data.appUser.password %}
                    Heslo bylo nastaveno při registraci.
                {% else %}
                    Heslo si nastavíte při aktivaci účtu.
                    Pokud heslo zapomenete, můžete jej obnovit pomocí tlačítka na přihlašovací stránce aplikace.
                {% endif %}
            </p>
        </td>
    </tr>
{% endblock %}




