{% extends '@ZakjakubOswisCore/e-mail/message.html.twig' %}
{% block title %}
    {% if type == 'activation-request' %}
        Vytvoření uživatelského účtu
    {% elseif type == 'activation' %}
        Aktivace uživatelského účtu
    {% else %}
        Změna uživatelského účtu
    {% endif %}
{% endblock %}
{% block description %}
    {% if type == 'activation-request' %}
        Vytvoření uživatelského účtu
    {% elseif type == 'activation' %}
        Aktivace uživatelského účtu
    {% else %}
        Změna uživatelského účtu
    {% endif %}
{% endblock %}
{% block content_content %}
    <tr>
        <td colspan="2">
            <p>
                Dobrý den,

                {% if type == 'activation-request' %}
                    byl vytvořen nový uživatelský účet v informačním systému.<br>
                {% elseif type == 'activation' %}
                    Váš uživatelský účet byl aktivován.<br>
                {% else %}
                    došlo ke změně u Vašeho uživatelského účtu v informačním systému.
                {% endif %}
            </p>

            {% if type == 'activation-request' and token %}
                Ještě je nutné provés aktivaci uživatelského účtu. Aktivaci provedete na následujícím odkazu:
                <a href="{{ absolute_url(path('zakjakub_oswis_core_app_user_activation', {'token': token})) }}" title="Odkaz pro aktivaci účtu"
                   target="_blank">{{ absolute_url(path('app-user-activation', {'token': token})) }}</a>.
                Odkaz je platný 12 hodin od vygenerování.
            {% endif %}

            <p>
                Uživatelské jméno u uživatelského účtu je
                <strong>{{ appUser.username ? appUser.username : appUser.email }}</strong>
                a hlavní e-mail je nastaven na <strong>{{ appUser.email }}</strong>.
                {% if password %}
                    Bylo nastaveno nové náhodně vygenerované heslo <strong>{{ password }}</strong>.
                {% elseif appUser.password %}
                    Heslo bylo nastaveno při registraci.
                {% else %}
                    Heslo si nastavíte při aktivaci účtu.
                    Pokud heslo zapomenete, můžete jej obnovit pomocí tlačítka na přihlašovací stránce aplikace.
                {% endif %}
            </p>
        </td>
    </tr>
{% endblock %}




